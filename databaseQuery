UPDATE comments SET comment_text = 'Nigeria is the best' WHERE id = 1;
DELETE FROM users WHERE id = 1;
SELECT * FROM users INNER JOIN comments ON users.id = comments.user_id;
INSERT INTO comments (username, comment_text, user_id) VALUE ('fidphina', 'I am a developer, just created my first database', 1);

